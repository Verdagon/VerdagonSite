next-decade-languages-rc

maybe call it "new approaches to memory safety"

talk about type stability

talk about how RC might get faster, both from uni inline data, and from the region borrow checker

talk about generational references

talk about basil's weird stack thing


nim, orca. perceus?

maybe also lament that swift shared its objects across threads, otherwise it would be in an awesome position to take advantage of this.
